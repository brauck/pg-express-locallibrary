extends layout

block content
  h1= title

  ul
    each author in author_list
      li
        a(href=author_url(author)) #{author.family_name}, #{author.first_name}
        |  (#{lifespan(author)})
        //-|  (#{author.date_of_birth_formatted} - #{author.date_of_death_formatted})
        //|  #{author.lifespan}

    else
      li There are no authors.

