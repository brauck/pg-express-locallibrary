extends layout

block content
  h1= title

  ul
    each genre in genre_list
      li
        a(href=genre_url(genre)) #{genre.name}
        //-|  (#{author.date_of_birth} - #{author.date_of_death})
        //-|  (#{author.date_of_birth_formatted} - #{author.date_of_death_formatted})

    else
      li There are no genres.
